[package]
name = "rados-primitives"
version = "0.1.0"
edition = "2024"
authors = ["matcha"]
description = "Guarded single-object operations, leases, watch and writer election on RADOS."
license = "Apache-2.0"

[dependencies]
librados = { path = "../ceph-rust" }
thiserror = "1.0"

[dev-dependencies]
uuid = { version = "1.6", features = ["v4"] }
